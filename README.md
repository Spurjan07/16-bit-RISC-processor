# 16-bit-RISC-processor
This project implements a simple 16-bit CPU using Verilog HDL that performs basic arithmetic operations like ADD, SUB, and ADDI. It follows a single-cycle fetch–decode–execute model and includes a stack mechanism to trace intermediate results. The design is verified through simulation using a custom testbench.
